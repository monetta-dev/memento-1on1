# ============================================
# Production Environment (Example)
# Note: This file is for reference only.
# Actual values should be set in your deployment platform (Coolify).
# ============================================

# Supabase Configuration (Production)
# Get these from your Supabase project dashboard: Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase Google OAuth Configuration (Production)
# Create a separate OAuth client in Google Cloud Console for production
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=your-production-google-client-id.apps.googleusercontent.com
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=your-production-google-client-secret

# Site URL for OAuth redirects (Production domain)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# LiveKit Configuration (Production)
# Get these from your LiveKit cloud dashboard
NEXT_PUBLIC_LIVEKIT_URL=wss://your-livekit-domain.livekit.cloud
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# AI Configuration (Production)
# Use production API keys with appropriate usage limits
GEMINI_API_KEY=your-gemini-production-api-key
DEEPGRAM_API_KEY=your-deepgram-production-api-key

# LINE Configuration (Production)
# Update LINE Developer Console with production redirect URIs
LINE_LOGIN_CHANNEL_ID=your-production-line-channel-id
LINE_LOGIN_CHANNEL_SECRET=your-production-line-channel-secret
LINE_MESSAGING_ACCESS_TOKEN=your-production-line-access-token
LINE_REDIRECT_URI=https://your-domain.com/api/line/callback
LINE_SITE_URL=https://your-domain.com

# Security Note: Never commit real production keys to version control.
# Use environment variables in your deployment platform.